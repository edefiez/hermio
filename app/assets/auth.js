/*
 * Authentication pages JavaScript entry point
 *
 * This entry point is used for login and registration pages.
 * It includes only the necessary assets for authentication.
 */

// Import authentication page styles
import './styles/auth.scss';

// Import Bootstrap JS (for interactive components like dropdowns, tooltips)
import 'bootstrap';

// Import auth illustration (Webpack will process and copy it)
import './images/auth-illustration.svg';

console.log('Hermio auth pages loaded');
