# Environment variables for test environment
APP_ENV=test
APP_SECRET=test_secret_key_for_testing_only
APP_DEBUG=1

# Database configuration for tests
DATABASE_URL="postgresql://hermio:hermio@db:5432/hermio_test?serverVersion=16&charset=utf8"

# Mailer configuration (use null transport for tests)
MAILER_DSN=null://null

# Default URI for tests (used by QuotaExceptionSubscriber and SubscriptionController)
DEFAULT_URI=http://localhost

# Stripe configuration for tests (dummy values)
STRIPE_SECRET_KEY=sk_test_dummy_key_for_testing
STRIPE_WEBHOOK_SECRET=whsec_test_dummy_secret_for_testing

# Kernel class for tests
KERNEL_CLASS=App\Kernel
