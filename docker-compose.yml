services:
  app:
    build:
      context: .
      dockerfile: devops/docker/app/Dockerfile
    volumes:
      - ./app:/app:cached
      - app_var:/app/var
    environment:
      APP_ENV: dev
      APP_DEBUG: 1
      DATABASE_URL: mysql://root:azerty@db:3306/hermio?serverVersion=mariadb-11.4.3
      REDIS_DSN: redis://redis:6379/0
      MESSENGER_TRANSPORT_DSN: redis://redis:6379/messages
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  nginx:
    build:
      context: .
      dockerfile: devops/docker/nginx/Dockerfile
    volumes:
      - ./app:/app:cached
    depends_on:
      - app

  db:
    image: mariadb:11.4
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: azerty
      MYSQL_DATABASE: hermio
      MYSQL_USER: symfony
      MYSQL_PASSWORD: symfony
    volumes:
      - mariadb_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost", "-uroot", "-pazerty"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:alpine
    command: ["redis-server", "--save", "", "--appendonly", "no"]
    volumes:
      - redis_data:/data

  phpmyadmin:
    image: phpmyadmin:latest
    depends_on:
      - db
    environment:
      PMA_HOST: db
      PMA_USER: root
      PMA_PASSWORD: azerty
      UPLOAD_LIMIT: 100M              # ✅ augmente la limite d’upload
      MEMORY_LIMIT: 256M              # (optionnel)
      POST_MAX_SIZE: 100M             # (optionnel)

  mailer:
    image: axllent/mailpit
    ports:
      - "1125"
      - "8025"
    environment:
      MP_SMTP_AUTH_ACCEPT_ANY: 1
      MP_SMTP_AUTH_ALLOW_INSECURE: 1

volumes:
  app_var:
  mariadb_data:
  redis_data:
